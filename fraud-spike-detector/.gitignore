__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Generated data is large and fully reproducible via generate_data.py (seeded RNG),
# so we don't commit it -- but we keep the final metrics and report images.
data/*.csv

# Trained models (reproducible via detect.py)
models/*.joblib

# Keep reports (metrics + visualizations)
!reports/metrics.csv
!reports/*.png
